# 採点ワーカー（Container Apps / Consumption）のデプロイ依存。
#
# デプロイ単位ごとに要るものだけを書く。イメージには core/ と scoring/ を同梱する
# （どちらもリポジトリ内のパッケージなので、ここには書かない）。
#
# scoring/ は標準ライブラリのみに依存させる規約（CLAUDE.md）。
# その規約は scoring/ にだけ掛かるもので、core/ 以降には掛からない。

h3==4.5.0
psycopg[binary]==3.3.4
